{
  "name": "zero-flux-web",
  "version": "1.0.0",
  "description": "Zeroflux Technologies - IT Support & System Admin",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "build": "esbuild analytics.js --bundle --outfile=vercel-analytics.js --format=iife --platform=browser",
    "build:analytics": "esbuild analytics.js --bundle --outfile=vercel-analytics.js --format=iife --platform=browser"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
